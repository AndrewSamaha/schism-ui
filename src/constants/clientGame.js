const defaultStartingLocation = [8, -15, 20]; // z= -15.5 for a zoomed-out view //[-10, -10, 5.5]; // 0,0,2.5 is normal game height, -20.5 lets you see a lot
const visibilityRange = 3;
const fogOfWarDelay_ms = 300000;

export {
    defaultStartingLocation,
    fogOfWarDelay_ms,
    visibilityRange
};
