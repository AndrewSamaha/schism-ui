const defaultStartingLocation = [0, 0, 0.5]; // 2.5 is normal game height, -20.5 lets you see a lot
const visibilityRange = 3;
const fogOfWarDelay_ms = 300000;

export {
    defaultStartingLocation,
    fogOfWarDelay_ms,
    visibilityRange
};
